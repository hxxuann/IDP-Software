import time


def collect(point):
    #collect parcel from points ABCD
    pass

def deposit():
    #deposit parcel at depots
    pass

tasks = ['A', 'B', 'C', 'D']

while True:
    for i in tasks:
        if timer_expired:
            break
        collect(i)
        deposit()
